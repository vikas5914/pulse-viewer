{
  "name": "pulse-listen",
  "module": "src/cli.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "go build -C cmd/pulse-lzfse -o ../../bin/pulse-lzfse.exe .",
    "listen": "bun run src/cli.ts listen",
    "probe": "bun run src/cli.ts probe",
    "test": "bun test"
  },
  "dependencies": {
    "bonjour-service": "^1.4.2"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
